document.getElementById("registrationForm").addEventListener("submit", function (event) {
    // Tambahkan logika validasi jika diperlukan
    // Contoh: cek apakah semua input telah diisi dengan benar
});
